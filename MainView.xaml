<Window x:Class="Lab_5_1_Button.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab_5_1_Button"
             mc:Ignorable="d" 
             Height="150" Width="400"
        Title="ЗАДАНИЕ 5.1 СОЗДАНИЕ КНОПОК">
    
    <StackPanel>
       
        <Button Content="Количество труб" Command="{Binding PipeCountCommand}" Margin="5"/>
        <Button Content="Объем всех стен" Command="{Binding WallVolumeCommand}" Margin="5"/>
        <Button Content="Количество дверей" Command="{Binding DoorsCountCommand}" Margin="5"/>

    </StackPanel>
</Window>
